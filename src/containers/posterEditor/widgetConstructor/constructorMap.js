import { BackgroundWidget, RectWidget, LayoutWidget, CommonWidget } from './index'

export default {
    image: CommonWidget,
    text: CommonWidget,
    background: BackgroundWidget,
    rect: CommonWidget,
    layout: LayoutWidget
}
